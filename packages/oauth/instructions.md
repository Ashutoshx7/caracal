# oauth

## Scope
- Covers the `@caracalai/oauth` package; per-language implementations live in subdirectories.

## Required
- Each language implementation must live in its own subdirectory (`ts/`, `go/`, `python/`).

## Forbidden
- Must not contain implementation code at this level.
